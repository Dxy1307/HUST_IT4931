apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: hadoop-datanode
  name: hadoop-datanode
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
